name: pipeline

# The hands-off loop: crawl all sources, score, send instant alerts, and commit the
# updated SQLite back to the repo (the "git scraping" state). Every 2h during waking
# hours (~08:00–18:00 Prague = 06:00–16:00 UTC in summer); the 08:00 run flushes anything
# posted overnight. notify also self-guards quiet hours (19:00–08:00), so a stray run
# never pings at night regardless of schedule/DST.
on:
  schedule:
    - cron: "0 6-16/2 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: pipeline          # never let two runs commit state at once
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - run: pip install -r requirements.txt

      - name: ingest -> score -> notify
        env:
          MAPY_API_KEY: ${{ secrets.MAPY_API_KEY }}
          GOOGLE_MAPS_API_KEY: ${{ secrets.GOOGLE_MAPS_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          python run.py ingest --pages 6
          python run.py score
          python run.py notify

      - name: commit state
        run: |
          git config user.name "flat-hunt-bot"
          git config user.email "actions@users.noreply.github.com"
          git add -f data/flats.db
          git commit -m "state: $(date -u +%Y-%m-%dT%H:%MZ)" || { echo "no state change"; exit 0; }
          git pull --rebase --autostash
          git push
